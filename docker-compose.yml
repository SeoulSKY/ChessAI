version: "3.9"

services:
  ai-server:
    build: ./ai-server
    container_name: ai-server
    expose:
      - 80
  web-server:
    build: ./web-server
    container_name: web-server
    depends_on:
      - ai-server
    ports:
      - "8080:80"
